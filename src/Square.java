import java.awt.*;

public class Square extends Rectangle{
    public Square(Point position, Color color, Color fillColor, double length) {
        super(position, color, fillColor, length, length);
    }
}
